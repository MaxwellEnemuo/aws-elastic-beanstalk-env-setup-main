export enum vpcIds {
  STAGING = 'vpc-02705xxx',
  PRE_PRODUCTION = 'vpc-042bexxx',
  PRODUCTION = 'vpc-f135exxx',
}

export enum keyNames {
  'STAGING' = 'firsta-staging',
  'PRE_PRODUCTION' = 'pre-production',
  'PRODUCTION' = 'production',
}
